#pragma once

/*
	Condition
	
	used at table query, it is a interface of table query qualifier.
	it qualifies on each record (row).

	WhereExpression implements this class.

*/
class Condition
{
public:
	Condition();
	~Condition();

private:

};

